version: '2'
services:
  elasticsearch:
    image: elasticsearch:6.1.1
    ports:
      - "9200:9200"
      - "9300:9300"
    environment:
      - METHOD=aes-256-cfb
      - PASSWORD=nosmokinggoogle
    restart: always
  
  elasticsearch-head:
    image: mobz/elasticsearch-head:5-alpine
    ports:
      - "9100:9100"
    restart: always
